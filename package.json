{
  "scripts": {
    "start": "npx concurrently \"cd frontend && yarn start\" \"cd backend && yarn start\"",
    "start:debug": "concurrently \" cd backend && yarn start:debug\" \"cd frontend && yarn start\""
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
